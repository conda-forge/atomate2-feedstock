{% set name = "atomate2" %}
{% set version = "0.1.3" %}
{% set python_min = "3.11" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.org/packages/source/{{ name[0] }}/{{ name }}/atomate2-{{ version }}.tar.gz
  sha256: 9f3716a77a8c0d9af4b215e940a07d16998406b5522f5380c87d95dbe59926a7

build:
  entry_points:
    - atm = atomate2.cli:cli
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation
  number: 0

requirements:
  host:
    - python {{ python_min }}
    - setuptools >=42
    - versioningit >=1.0,<2.dev0
    - wheel
    - pip
  run:
    - pymongo <=4.16.0
    - python >={{ python_min }}
    - pyyaml
    - click
    - custodian >=2024.4.18
    - emmet-core >=0.86.1
    - jobflow >=0.1.11
    - monty >=2024.12.10
    - numpy
    - pydantic-settings >=2.0.3
    - pydantic >=2.0.1
    - pymatgen >=2024.11.13
    - maggma >=0.72.0

test:
  imports:
    - atomate2
  commands:
    - pip check
    - atm --help
  requires:
    - pip
    - python {{ python_min }}

about:
  home: https://materialsproject.github.io/atomate2/
  summary: atomate2 is a library of materials science workflows
  license: BSD-3-Clause
  license_file: LICENSE

extra:
  recipe-maintainers:
    - jan-janssen
