{% set name = "atomate2" %}
{% set version = "0.1.0" %}
{% set python_min = "3.11" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.org/packages/source/{{ name[0] }}/{{ name }}/atomate2-{{ version }}.tar.gz
  sha256: c41cbd4b0183abe2ea03fb7bf24f522c77bc8fa3f3a1c717d4300cb48be8a4ca

build:
  entry_points:
    - atm = atomate2.cli:cli
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation
  number: 0

requirements:
  host:
    - python {{ python_min }}
    - setuptools >=42
    - versioningit >=1.0,<2.dev0
    - wheel
    - pip
  run:
    - pymongo <=4.16.0
    - python >={{ python_min }}
    - pyyaml
    - click
    - custodian >=2024.4.18
    - emmet-core >=0.86.1
    - jobflow >=0.1.11
    - monty >=2024.12.10
    - numpy
    - pydantic-settings >=2.0.3
    - pydantic >=2.0.1
    - pymatgen >=2024.11.13

test:
  imports:
    - atomate2
  commands:
    - pip check
    - atm --help
  requires:
    - pip
    - python {{ python_min }}

about:
  home: https://materialsproject.github.io/atomate2/
  summary: atomate2 is a library of materials science workflows
  license: BSD-3-Clause
  license_file: LICENSE

extra:
  recipe-maintainers:
    - jan-janssen
